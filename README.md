# agent-test-repo

## Running locally

To run the service locally, follow these steps:

1. Clone the repository.
2. Navigate to the project directory.
3. Run `python src/app.py` to start the service.
4. Open your browser and go to `http://localhost:8080` to see the status.